# 분산 동기화 시스템 아키텍처 설계

## 요구사항 분석

### 핵심 요구사항

1. **문서 인덱싱 완료 알림**
   - 벡터 임베딩 파이프라인 완료 시 팝업 알림
   - 벡터DB, 인덱스, RAG 데이터 변경 시그널

2. **델타 전송 시스템**
   - 변경된 데이터만 추출 (델타)
   - 전송 파일 생성 및 다운로드
   - 증분 설치 및 업데이트

3. **역할 기반 시스템**
   - 메인서버 역할
   - 선박클라이언트 역할
   - 애플리케이션에서 역할 설정

4. **통신 프로토콜**
   - HTTP/2를 통한 파일 전송
   - WebSocket을 통한 실시간 동기화
   - 양방향 통신

5. **UI 구성**
   - 메인서버 페이지 (AI 챗봇)
   - 선박클라이언트 페이지 (AI 챗봇)
   - 역할 설정 페이지

## 시스템 아키텍처

### 1. 역할 관리 시스템

```
┌─────────────────────────────────────┐
│      애플리케이션 설정               │
│  - 시스템 역할: 메인서버/선박클라이언트 │
│  - 연결 정보: IP, Port, 인증        │
└─────────────────────────────────────┘
```

### 2. 델타 감지 및 생성

```
문서 인덱싱 완료
    ↓
변경 감지 (CDC)
    ↓
델타 데이터 추출
    ↓
델타 파일 생성 (ZIP)
    ↓
팝업 알림
```

### 3. 전송 시스템

```
팝업 알림
    ↓
[즉시 전송] [스케줄 전송]
    ↓
HTTP/2 파일 전송
    ↓
WebSocket 상태 동기화
    ↓
수신 측 증분 설치
```

## 핵심 개선 포인트

### 1. 현재 시스템의 한계
- RAG 동기화가 수동 방식
- 델타 감지 미구현
- 실시간 동기화 없음
- 역할 기반 시스템 없음

### 2. 개선 방향
- 자동 델타 감지 및 생성
- 실시간 동기화 (WebSocket)
- 역할 기반 자동 전송
- 증분 업데이트 시스템

## 솔루션 시나리오

### 1단계: 시스템 역할 관리 구현
- 시스템 역할 설정 (메인서버/선박클라이언트)
- 연결 정보 관리
- 역할별 UI 분리

### 2단계: 델타 감지 및 생성 시스템
- 문서 인덱싱 완료 이벤트 감지
- 변경된 청크만 추출 (CDC 기반)
- 델타 파일 생성 (벡터, 메타데이터, 인덱스)

### 3단계: 팝업 알림 시스템
- 인덱싱 완료 시 프론트엔드 알림
- 전송 옵션 선택 (즉시/스케줄)
- 전송 진행 상황 표시

### 4단계: 전송 시스템 구현
- HTTP/2 파일 전송 엔드포인트
- WebSocket 실시간 상태 동기화
- 전송 큐 관리 (즉시/스케줄)

### 5단계: 수신 및 증분 설치
- 델타 파일 다운로드
- 검증 및 설치
- 벡터DB 증분 업데이트
- 인덱스 재구성

### 6단계: 양방향 동기화
- 메인서버 ↔ 선박클라이언트
- 변경 사항 자동 감지
- 충돌 해결 메커니즘

## 기대 효과

1. **자동화**: 수동 동기화 → 자동 델타 전송
2. **효율성**: 전체 동기화 → 변경분만 전송 (네트워크 대역폭 절약)
3. **실시간성**: 배치 동기화 → 실시간 동기화
4. **안정성**: 전송 실패 시 재시도 및 검증

## 구현 계획

다음 단계에서 상세 구현 계획을 수립하겠습니다.

