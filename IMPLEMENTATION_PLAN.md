# 분산 동기화 시스템 구현 계획

## 전체 구현 단계

### Phase 1: 시스템 역할 관리 (1단계)

#### 1.1 데이터베이스 모델 추가
- `SystemRole` 모델: 시스템 역할 및 연결 정보
- 역할: `main_server`, `ship_client`
- 연결 정보: IP, Port, 인증 토큰

#### 1.2 설정 관리
- 시스템 역할 설정 API
- 연결 정보 저장 및 관리
- 역할별 기능 활성화/비활성화

#### 1.3 UI 구현
- 시스템 설정 페이지
- 역할 선택 및 연결 정보 입력

### Phase 2: 델타 감지 시스템 (2단계)

#### 2.1 이벤트 시스템
- 문서 인덱싱 완료 이벤트 발행
- WebSocket을 통한 실시간 알림
- 프론트엔드 이벤트 수신

#### 2.2 델타 추출
- 변경된 문서/청크 감지
- 마지막 동기화 이후 변경분만 추출
- 델타 메타데이터 생성

### Phase 3: 델타 파일 생성 (3단계)

#### 3.1 델타 패키지 생성
- 변경된 벡터 임베딩 추출
- 변경된 메타데이터 추출
- 인덱스 델타 생성
- ZIP 파일로 패키징

#### 3.2 검증 및 서명
- 델타 파일 무결성 검증
- 체크섬 생성
- 메타데이터 포함

### Phase 4: 팝업 알림 시스템 (4단계)

#### 4.1 프론트엔드 알림
- WebSocket 이벤트 수신
- 팝업 다이얼로그 표시
- 전송 옵션 선택

#### 4.2 전송 옵션
- 즉시 전송: 바로 전송 시작
- 스케줄 전송: 예약 시간 설정

### Phase 5: 전송 시스템 (5단계)

#### 5.1 HTTP/2 파일 전송
- 델타 파일 업로드 엔드포인트
- 청크 단위 전송 (대용량 파일)
- 진행률 추적

#### 5.2 WebSocket 동기화
- 실시간 상태 업데이트
- 전송 진행률 스트리밍
- 오류 알림

#### 5.3 전송 큐 관리
- 즉시 전송 큐
- 스케줄 전송 큐
- 재시도 메커니즘

### Phase 6: 수신 및 증분 설치 (6단계)

#### 6.1 델타 파일 다운로드
- HTTP/2 다운로드 엔드포인트
- 진행률 추적
- 검증

#### 6.2 증분 설치
- 델타 파일 압축 해제
- 벡터DB 증분 업데이트
- 인덱스 재구성
- 검증 및 롤백

### Phase 7: 역할별 UI (7단계)

#### 7.1 메인서버 페이지
- AI 챗봇 인터페이스
- 동기화 상태 모니터링
- 전송 이력

#### 7.2 선박클라이언트 페이지
- AI 챗봇 인터페이스
- 수신 상태 모니터링
- 동기화 이력

### Phase 8: 양방향 동기화 (8단계)

#### 8.1 충돌 감지
- 동시 변경 감지
- 버전 관리
- 충돌 해결 전략

#### 8.2 양방향 전송
- 메인서버 → 선박클라이언트
- 선박클라이언트 → 메인서버
- 자동 동기화 스케줄

## 구현 우선순위

1. **High Priority**: Phase 1, 2, 3, 4 (기본 기능)
2. **Medium Priority**: Phase 5, 6 (전송 시스템)
3. **Low Priority**: Phase 7, 8 (고급 기능)

## 기술 스택

- **백엔드**: FastAPI, WebSocket, HTTP/2
- **프론트엔드**: React, WebSocket Client
- **데이터베이스**: PostgreSQL/SQLite
- **벡터DB**: Chroma
- **압축**: ZIP, tar.gz

## 다음 단계

이제 Phase 1부터 순차적으로 구현을 시작하겠습니다.

